---
context: default
phase: curso-sdr-sesion-01
task: 5
total_tasks: 10
status: in_progress
last_updated: 2026-09-09T00:07:45.468Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Saltar la puerta de sesión | S02 se abriría con S01 aún en revisión | advisory | Antes de crear `docs/sessions/02-*/`, comprobar que S01 ya no tiene `status: draft`. |
| `mkdocs build` desde subcarpeta | Ya falló al ejecutarse desde `figures/` porque no encontró `mkdocs.yml` | advisory | Ejecutar siempre `mkdocs build --strict` desde la raíz o con `-f mkdocs.yml`. |
| `gnuradio.__version__` | La guía publicó un comando inexistente y el profesor lo detectó al probar | advisory | Usar `from gnuradio import gr; print(gr.version())` o `gnuradio-config-info --version`; probar comandos antes de publicar. |
| Premisa técnica sin fuente | Se asumió erróneamente que VirtualBox 7 requería Extension Pack para USB 2/3 | advisory | Contrastar hechos técnicos con fuentes primarias antes de incorporarlos a una guía. |
| URL local sin prefijo | El sitio servido localmente devuelve 404 si se omite `/sdr/` | advisory | Verificar con `http://127.0.0.1:9000/sdr/...` cuando el servidor use el `site_url` configurado. |
| `import gnuradio.rds` | `gr-rds` de radioconda se importa como `import rds` | advisory | Confirmar los nombres reales en el entorno radioconda. |
| Cuenta GitHub equivocada | El primer push creó un repo público huérfano bajo `ollerenac/sdr` | advisory | Verificar `gh auth status` y que la cuenta activa sea `ollerenac-uni` antes de cualquier operación externa. |
| Leer `.cu8` como `complex` | `test.grc` apunta un File Source complejo directamente a un archivo raw uint8 I/Q | advisory | Usar RTL-SDR Source con `file=...` o convertir a `complex64/.cfile`; nunca cambiar solo la extensión. |
| Editar GRC sin regenerar Python | `test.grc` contiene Throttle pero `test.py` no; el GRC es más reciente | advisory | Tratar `.grc` como fuente, regenerar `.py` con GNU Radio 3.10.12 y revisar el diff antes de commit. |
| Runtime GSD incompleto | `~/.agents/gsd-core/bin/gsd-tools.cjs` falló con `MODULE_NOT_FOUND ../../../package.json` | advisory | Usar temporalmente `~/.codex/gsd-core/bin/gsd-tools.cjs` o reparar la instalación de `~/.agents`. |

<current_state>
Repositorio del curso abierto de SDR de la UNI, publicado con MkDocs/GitHub Pages. No hay una fase GSD formal; el plan operativo sigue en `docs/plans/2026-09-05-curso-sdr.md`, tarea 5 de 10.

S01 está completa en estructura pero continúa en `draft` y revisión. El profesor tiene cambios locales no confirmados en la introducción y sección 1: reorganizó prosa en viñetas y dejó pedidos entre corchetes para (1) imágenes que comparen arquitecturas según aplicación, (2) referencias con diagramas de RF sampling/Zero-IF/Low-IF y (3) una imagen del dongle abierto. Estas notas son del profesor: preservarlas hasta resolverlas explícitamente.

Hay además un experimento local en `gnuradio-flowgraphs/test.grc` para reproducir `/home/researcher/fm_muestra.cu8`. El GRC usa File Source tipo complex y Throttle, pero el `.cu8` real contiene bytes uint8 I/Q y el `test.py` generado no incluye Throttle. No considerar este flowgraph validado.

Git: `main` en `67282e7`, un commit por delante de `origin/main`. Antes del handoff había tres archivos modificados: S01, `test.grc` y `test.py`.
</current_state>

<completed_work>

Completed Tasks:
- Tarea 1: fijar MkDocs/Material - Done
- Tarea 2: pestaña Guías e índice de setup - Done
- Tarea 3: guía radioconda - Done
- Tarea 4: guía RTL-SDR - Done
- Tarea 5: portada e índice de sesiones - In progress; faltan syllabus y evaluación definitiva
- S01: teoría, seis figuras, ejercicios, laboratorio y R0 - In progress; en revisión del profesor
- Auditoría 2026-09-08: estructura, stack, estado y riesgos revisados sin modificar fuentes - Done
- Verificación: `mkdocs build --strict` pasó y los tres `.py` parsean correctamente - Done
</completed_work>

<remaining_work>

- Preservar y resolver las tres solicitudes visuales añadidas por el profesor en S01.
- Completar la explicación espectral ①–⑦ y aprobar S01 quitando `status: draft`.
- Corregir `test.grc`: no abrir `.cu8` como complex; eliminar la ruta absoluta portablemente; regenerar `test.py` y probarlo.
- Completar `docs/syllabus.md` y alinear `docs/exams/index.md` con el proyecto final de S16.
- Crear `sdr-reportes-template` y `docs/setup/sitio-reportes.md` antes de S02.
- Crear issue template de instalación.
- Crear `sdr-datasets` y grabar muestras IQ oficiales.
- Mantener S02 bloqueada hasta que S01 deje de estar en draft.
</remaining_work>

<decisions_made>

- radioconda 2025.03.14/GNU Radio 3.10.12 es el entorno principal multiplataforma.
- Todos los laboratorios se califican desde archivos IQ oficiales; el dongle es opcional.
- Evaluación: parcial 30 %, reportes 30 %, proyecto por hitos 40 %.
- Cada sesión nace como draft y bloquea la siguiente hasta aprobación.
- Diagramas y figuras deben ser propios o tener licencia compatible; no copiar resultados de Google ni material de rtl-sdr.com sin permiso.
- `.grc` es la fuente de verdad; `.py` generado debe regenerarse después de editar el flowgraph.
- Para raw `.cu8`, usar RTL-SDR Source con argumentos de archivo o convertir explícitamente a complex64 antes de File Source complex.
</decisions_made>

<blockers>
- S01 está en revisión humana y bloquea S02.
- Las imágenes solicitadas requieren localizar fuentes primarias/licencias compatibles o producir diagramas propios.
- El flowgraph de archivo IQ no es ejecutable de forma confiable hasta corregir formato y regenerar Python.
- Reporte 0 depende del repo plantilla, todavía inexistente.
- Los laboratorios sin dongle dependen de grabaciones IQ todavía inexistentes.
- Crear los repos públicos requiere confirmación explícita del profesor.
- El runtime GSD de `~/.agents` está incompleto; usar `~/.codex/gsd-core` hasta repararlo.
</blockers>

## Required Reading (in order)

1. `docs/plans/2026-09-05-curso-sdr-design.md` — diseño y syllabus aprobados.
2. `docs/plans/2026-09-05-curso-sdr.md` — plan de 10 tareas y regla de puerta.
3. `git diff -- docs/sessions/01-introduccion-sdr/index.md` — cambios actuales del profesor que deben preservarse.
4. `docs/sessions/01-introduccion-sdr/index.md` — sesión en revisión y solicitudes visuales.
5. `docs/setup/rtl-sdr.md` líneas 196–226 — contrato exacto del formato `.cu8` y formas válidas de reproducirlo.
6. `gnuradio-flowgraphs/test.grc` y `test.py` — experimento desincronizado que necesita corrección.
7. `.planning/HANDOFF.json` — estado estructurado, acciones humanas y detalles de archivos no confirmados.

## Critical Anti-Patterns (do NOT repeat these)

- No descartar, reformatear ni completar silenciosamente las notas entre corchetes del profesor.
- No copiar imágenes encontradas en Google sin confirmar licencia; redibujar o usar fuentes compatibles.
- No ejecutar ni presentar `test.grc` como correcto mientras File Source complex apunte a `.cu8`.
- No editar `test.py` como fuente principal; corregir el GRC y regenerarlo.
- No incluir los tres archivos de trabajo del profesor en un commit incidental de documentación GSD.
- No iniciar S02 mientras S01 tenga `status: draft`.
- No usar el runtime roto de `~/.agents/gsd-core` hasta repararlo.

## Infrastructure State

- Sitio: `https://ollerenac-uni.github.io/sdr/`; despliegue por GitHub Actions al hacer push a `main`.
- Build local auditado: MkDocs 1.6.1 sobre Python 3.10; `mkdocs build --strict` exitoso el 2026-09-08.
- Entorno del curso: radioconda 2025.03.14, GNU Radio 3.10.12.
- No se detectaron servidores MkDocs/GNU Radio relevantes ni jobs externos activos al pausar.
- No hay pruebas automatizadas ni notebooks todavía; el gate actual es build estricto más validación manual.
- Git: `main` un commit por delante de `origin/main`; no hacer push sin petición del profesor.
- Runtime GSD funcional: `/home/researcher/.codex/gsd-core/bin/gsd-tools.cjs`.
- Runtime GSD roto: `/home/researcher/.agents/gsd-core/bin/gsd-tools.cjs` (`MODULE_NOT_FOUND`).

<context>
El repositorio es contenido docente, no una aplicación. La revisión realizada fue de solo lectura y confirmó que la arquitectura del sitio es sencilla y saludable, pero el producto está incompleto: solo S01 está desarrollada. El trabajo inmediato está en dos hilos locales no confirmados: edición pedagógica de S01 y prueba de reproducción de una muestra FM. La edición de S01 contiene intención humana explícita y debe guiar el siguiente ciclo. El flowgraph revela un error de representación de datos: `.cu8` son bytes I/Q intercalados, mientras File Source complex espera complex64. Además, `test.grc` es más reciente que `test.py`, por lo que el Python está obsoleto.

Otros desajustes detectados: `docs/exams/index.md` habla de examen final aunque el diseño define proyecto/presentaciones; el README promete un `lab.ipynb` por sesión pero no existen notebooks; la raíz usa CC BY 4.0 mientras los Python generados llevan SPDX GPL-3.0, por lo que conviene aclarar el alcance de licencias.
</context>

<next_action>
Start with: leer el diff de S01 y presentar al profesor una propuesta concreta para las tres solicitudes visuales, usando diagramas propios o imágenes con licencia compatible. Preservar sus anotaciones hasta aprobación. Antes de probar el flowgraph, corregir la entrada `.cu8`, regenerar `test.py` desde `test.grc` y verificar ambos. No abrir S02 hasta aprobar S01.
</next_action>
